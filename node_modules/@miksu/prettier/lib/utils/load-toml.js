"use strict";

var parse = require("@iarna/toml/parse-string");

module.exports = function (filePath, content) {
  try {
    return parse(content);
  } catch (error) {
    error.message = "TOML Error in ".concat(filePath, ":\n").concat(error.message);
    throw error;
  }
};