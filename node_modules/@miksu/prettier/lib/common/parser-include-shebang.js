"use strict";

function includeShebang(text, ast) {
  if (!text.startsWith("#!")) {
    return;
  }

  var index = text.indexOf("\n");
  var shebang = text.slice(2, index);
  var comment = {
    type: "Line",
    value: shebang,
    range: [0, index],
    loc: {
      source: null,
      start: {
        line: 1,
        column: 0
      },
      end: {
        line: 1,
        column: index
      }
    }
  };
  ast.comments = [comment].concat(ast.comments);
}

module.exports = includeShebang;